# RabbitMQ 生产内存保护（源镜像；复刻实例可按主机内存调整）
# 内存达主机 20% 时触发背压/分页到磁盘，避免高峰积压撑爆内存导致 OOM
vm_memory_high_watermark.relative = 0.2
# 达到水位 50% 时即开始把队列消息分页到磁盘，平滑过渡而非硬阻塞
vm_memory_high_watermark_paging_ratio = 0.5
# 磁盘空间低于 1GB 时阻止发布，保护磁盘写满
disk_free_limit.absolute = 1GB
